# Guidelines for Contributors

- Run `go test ./...` in `api` and `pricer` when modifying Go code under these directories.
- Run `yarn lint` in `webui` when changing front-end files.
- After updating Docker or configuration files, run `make build` to verify images build.
