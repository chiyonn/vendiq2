services:
  front:
    container_name: dev-vendiq2-webui
    build:
      context: ./webui
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    command: yarn dev --host
    volumes:
      - ./webui:/app

